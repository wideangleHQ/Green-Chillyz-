generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added here as business modules are built.
// Each module owns its own models — see docs/21_Server_Architecture.md.
